# MandiPulse India local app configuration.

dashboard:
  mvp_entrypoint: streamlit_only
  port: 8501

mlflow:
  tracking_uri: sqlite:///mlruns/mlflow.db

artifacts:
  model_artifact_path: artifacts/models
  predictions_path: artifacts/predictions
  quality_reports_path: reports/data_quality

logging:
  level: INFO
